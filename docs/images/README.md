# Screenshots

Captured from a running stack and referenced from issues and documentation.
Kept in the repository so those references do not depend on a paste service
that will eventually go away.

| File | What it shows |
| ---- | ------------- |
| `object-browser-delete.png` | The object browser with a Delete action on every object row (ILS-45). |
